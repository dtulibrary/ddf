<div class="intro">
  <div class="container">
    <p class="pitch measure"> <%= t('blacklight.elevator_pitch')%> </p>
    <p class="news">
      <em class="news-tag"><%= t('blacklight.news_tag')%></em>
      <%= t('blacklight.news_content')%>
    </p>
  </div>
</div>

<div class="container">
  <!--   <div id="chart"></div> -->

  <ul>
    <% @types.each do |facet| %>
      <li>
        <a href="/"><%= facet[:label] %></a>
        <span class="count"><%= facet[:count] %></span>
        <span class="pct" style="width: 42%"><%= format_pct(facet[:pct]) %></span>
      </li>
    <% end %>
  </ul>
</div>

