<% search_block.with_solr_helper(self) %>

<h3>Publications in the Danish Research Database</h3>
<% if search_block.item_count > 10 %>
<h4>10 most recent of <%= search_block.item_count %> in total. </h4>
<% else %>
<h4>Most recent publications</h4>
<% end %>
<%# We need to populate a @response variable to make document actions work %>
<% @response ||= search_block.search_results %>
<%= render search_block.documents, as: 'document' %>
<%= link_to "Full publication list", catalog_index_path + search_block.search_url %>
